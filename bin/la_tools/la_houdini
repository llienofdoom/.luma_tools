#!/bin/bash

source $IJ_LOCAL_REPO/etc/houdini_env

case "$IJ_USER" in 
    mark  | neill | nvermaak | xchristophe | werner | divesh | william )
        houdinifx -n $@
        ;;
    * )
	# LD_LIBRARY_PATH=/opt/glibc-2.29/lib
    # LD_PRELOAD=/opt/glibc-2.29/lib/libm.so.6 
        houdinicore -n $@
        ;;
esac                                                                                           
